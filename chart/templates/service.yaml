apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki-mcp.fullname" . }}
  labels:
    {{- include "loki-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loki-mcp.selectorLabels" . | nindent 4 }}
