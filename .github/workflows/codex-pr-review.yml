name: Codex PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    permissions:
      contents: read
      pull-requests: write
      issues: write
      statuses: write
    uses: mvs5465-test/.github/.github/workflows/codex-pr-review.yml@main
    secrets: inherit
